(()=>{const $=s=>document.querySelector(s);const steps=['Right person','Open eMAR','Check allergies + warnings','Pop correct dose','Offer / administer','Confirm taken','Chart result'];let step=0,pills=0,modalOpen=false;const rail=$('#stepRail');function renderRail(){rail.innerHTML=steps.map((s,i)=>`<div class="step-chip ${i===step?'active':''} ${i<step?'done':''}"><b>${i+1}</b>${s}</div>`).join('');$('#echoNow').textContent=`${step+1}. ${steps[step]||'Pass complete'}.`;$('#echoList').innerHTML=steps.map((s,i)=>`<div class="echo-item ${i<step?'done':''}">${i<step?'✓':'○'} ${s}</div>`).join('')}function coach(msg){$('#medCoach').innerHTML=`<b>👀 COACH</b><p>${msg}</p>`}function setStep(n,title,text,msg){step=n;$('#stepTitle').textContent=title;$('#stepText').textContent=text;renderRail();if(msg)coach(msg)}function renderBlister(){const b=$('#blister');b.innerHTML='';for(let i=0;i<12;i++){const btn=document.createElement('button');btn.className='pill-pocket';btn.type='button';btn.innerHTML='<span></span>';btn.addEventListener('click',()=>{if(step<3){coach('Not yet. Open the eMAR and check the person, allergies, warnings, and order before preparing the medication.');return}if(btn.classList.contains('empty'))return;btn.classList.add('empty');pills++;renderCup();if(pills===2)setStep(4,'Dose prepared. Now administer.','You prepared the ordered dose. Offer/administer the medication to the person.','Nice. Two tablets are in the cup, matching the ordered dose. Move to administration.');else if(pills>2)coach('You now have more tablets in the cup than the ordered dose. Remove the extra tablet before administering.');});b.appendChild(btn)}renderCup()}function renderCup(){const c=$('#cupPills');c.innerHTML='';for(let i=0;i<pills;i++){const p=document.createElement('span');p.className='cup-pill';p.addEventListener('click',e=>{e.stopPropagation();pills--;renderCup();coach('Tablet removed from the cup. Re-check the ordered dose before moving on.')});c.appendChild(p)}$('#cupCount').textContent=`${pills} tablet${pills===1?'':'s'}`;$('#qty').value=pills}function openModal(){if(step===0){setStep(1,'Open the eMAR.','Use the current medication schedule before preparing anything.','Good start. You are using the eMAR instead of memory.')}modalOpen=true;$('#emarModal').hidden=false;if(step===1)setTimeout(()=>setStep(2,'Check the person, allergies, warnings, and order.','Confirm the record matches the person and review the medication details before preparation.','The person and eMAR match. No reported allergies are listed. A controlled-med warning is present for another medication, so remember counts when applicable.'),250)}function closeModal(){modalOpen=false;$('#emarModal').hidden=true}$('#administerBtn').addEventListener('click',openModal);$('#closeModal').addEventListener('click',closeModal);$('#cancelModal').addEventListener('click',closeModal);$('#medCup').addEventListener('click',()=>{if(pills>0){pills--;renderCup();coach('Tablet removed. Make sure the cup still matches the ordered dose.')}});$('#resultSelect').addEventListener('change',()=>{const v=$('#resultSelect').value;const hint=$('#noteHint');hint.textContent=v==='Taken'?'Usually not needed for a routine Taken dose.':`Add a clear administration note explaining the ${v} result.`;if(v!=='Taken'&&step<6)coach(`You selected ${v}. Make sure the result reflects what actually happened and add the needed context in Administration Notes.`)});$('#saveResult').addEventListener('click',()=>{const result=$('#resultSelect').value,qty=Number($('#qty').value),note=$('#adminNote').value.trim();if(result==='Taken'&&pills!==2){coach('Stop. The ordered dose is 2 tablets, but the prepared cup does not match. Correct the dose before saving Taken.');return}if(result!=='Taken'&&!note){coach(`Add an administration note explaining why the result is ${result}.`);return}if(step<4&&result==='Taken'){coach('You have not completed the preparation/administration steps yet. Do not chart ahead.');return}setStep(6,'Chart after administration.','Select the result that actually occurred and save it promptly.','You are charting after the pass—not ahead of it. That sequence matters.');$('#rowStatus').textContent=result;setTimeout(()=>{closeModal();step=7;rail.innerHTML=steps.map((s,i)=>`<div class="step-chip done"><b>${i+1}</b>${s}</div>`).join('');$('#echoNow').textContent='PASS COMPLETE ✓';$('#echoList').innerHTML=steps.map(s=>`<div class="echo-item done">✓ ${s}</div>`).join('');$('#stepTitle').textContent='Pass complete.';$('#stepText').textContent=`Result saved as ${result}.`;coach(result==='Taken'?'Nice pass. Right person, eMAR open, correct dose prepared, administered, then charted.':'You documented the exception with the correct result and supporting note.');},450)});document.querySelectorAll('.med-tabs button').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.med-tabs button').forEach(x=>x.classList.remove('active'));document.querySelectorAll('.tab-panel').forEach(x=>x.classList.remove('active'));b.classList.add('active');$('#tab-'+b.dataset.tab).classList.add('active')}));$('#newCase').addEventListener('click',()=>{step=0;pills=0;$('#resultSelect').value='Taken';$('#adminNote').value='';$('#rowStatus').textContent='Pending';$('#workspace')?.removeAttribute('hidden');renderBlister();setStep(0,'Start with the person and the eMAR.','Open the medication record before touching the medication.','Start by opening the eMAR. The medication stays put until you know what you are giving and to whom.')});renderRail();renderBlister();})();